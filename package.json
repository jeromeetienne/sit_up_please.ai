{
  "name": "sit-up-please-ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:chrome-extension": "vite build --config contribs/chrome_ext/vite.config.ts",
    "deploy": "npm run build && gh-pages -d dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@fontsource-variable/source-serif-4": "^5.3.0",
    "@mediapipe/tasks-vision": "^0.10.35",
    "bootstrap-icons": "^1.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
